{
  "name": "sosomedia-landing",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i css/input.css -o css/main.css --minify",
    "build:dev": "tailwindcss -i css/input.css -o css/main.css",
    "vercel-build": "npm run build && node scripts/copy-to-public.js"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
